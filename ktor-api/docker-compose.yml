version: "3.3"

services:
  web:
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      PORT: 10000
      JDBC_URL: "jdbc:sqlite:file:test?mode=memory&cache=shared"
    ports:
      - "10000:10000"